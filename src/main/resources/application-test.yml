spring:
  datasource:
    url: jdbc:mysql://localhost:3306/PETCLINIC_DB?useSSL=false&serverTimezone=UTC
    username: root
    password:



  sql:
    init:
      mode: always


  jpa:
    hibernate.ddl-auto: update # Crea la base de datos al iniciar y la elimina al finalizar
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect


logging:
  level:
    root: INFO
server:
  port: 8080
